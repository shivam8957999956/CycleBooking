<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cyclebooking">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PreviousBooking.PreviousBooking"></activity>
        <activity android:name=".Payment.Payment" />
        <activity android:name=".OnGoingBooking.OnGoingBooking" />
        <activity android:name=".BookANewCycle.completeBooking" />
        <activity android:name=".AllDetails.AllDetails" />
        <activity android:name=".UserLogin.UserHelperClass.Profile" />
        <activity android:name=".UserLogin.SignInActivity" />
        <activity android:name=".UserLogin.Login_activity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".BookANewCycle.BookANewCycleDashboard" />
        <activity android:name=".maindashbaord.MainDashboard" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>